# All Products LP — **Full catalog** (canonical)

This folder is **only** for the **full catalog** in `all-products.html`: `#full-catalog-grid`, text-first cards, blurple headings, value-prop bullets — **no** lockup sections with product image strips.

**Do not** replace this with the groups + imagery layout from Test A. These two folders stay separate forever.

**Sibling:** `All Products LP Test A` — groups + imagery only.
